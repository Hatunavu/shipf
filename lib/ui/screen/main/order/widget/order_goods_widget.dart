import 'package:flutter/material.dart';

class OrderGoodsWidget extends StatelessWidget {
  const OrderGoodsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Goods'),
    );
  }
}
